apiVersion: v1
kind: Service
metadata:
  name: multi-service-clusterip
spec:
  type: ClusterIP
  selector:
    app: multi-container-app
  ports:
  - name: nginx-port
    port: 9001
    targetPort: 80
    protocol: TCP
  - name: multitool-port
    port: 9002
    targetPort: 8080
    protocol: TCP