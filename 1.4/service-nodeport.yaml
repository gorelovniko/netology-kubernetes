apiVersion: v1
kind: Service
metadata:
  name: multi-service-nodeport
spec:
  type: NodePort
  selector:
    app: multi-container-app
  ports:
  - name: nginx-port
    port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP